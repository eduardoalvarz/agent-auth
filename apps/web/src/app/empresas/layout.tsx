export const metadata = {
  title: "about:blanc - empresas",
  description: "Opciones para empresas",
};

export default function RootLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return <>{children}</>;
}
